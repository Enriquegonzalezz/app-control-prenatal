{
    "private": true,
    "type": "module",
    "scripts": {
        "build": "composer install --no-dev --optimize-autoloader && npm install && vite build",
        "dev": "vite",
        "start": "php artisan serve --host=0.0.0.0 --port=${PORT:-8080}"
    },
    "devDependencies": {
        "autoprefixer": "^10.4.20",
        "axios": "^1.7.4",
        "concurrently": "^9.0.1",
        "laravel-vite-plugin": "^1.2.0",
        "postcss": "^8.4.47",
        "tailwindcss": "^3.4.13",
        "vite": "^6.0.11"
    }
}
